# R8.11
# Rechazar
# Crear un grafo, pero usar el conjunto de aristas donde se espera el conjunto de nodos.

DEF INTEGER main():
    Node nodeA = ("A");
    Node nodeB = ("B");
    
    SET nodes = {nodeA, nodeB};
    SET edges = {(nodeA, nodeB)};

    GRAPH graph = <edges, nodes>;

    RETURN 0;
