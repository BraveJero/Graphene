# R8.11
# Rechazar
# Crear un grafo, pero usar el conjunto de aristas donde se espera el conjunto de nodos.

START:
    CREATE GRAPH<EMPTY, EMPTY> g
    CREATE SET< EDGE<EMPTY> > newEdges
    INSERT INTO g NODE<EMPTY> newEdges
    