# R8.11
# Rechazar
# Crear un grafo, pero usar el conjunto de aristas donde se espera el conjunto de nodos.

START:
    CREATE GRAPH<EMPTY, EMPTY> g;
    CREATE SET<EDGES<EMPTY>> edges;
    INSERT INTO g NODES edges;  # Breaks
    INSERT INTO g NODE ("2");
    INSERT INTO g NODE ("1" -> "2");
