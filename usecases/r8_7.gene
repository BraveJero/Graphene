# R8.7
# Aceptar
# Crear 2 conjuntos de nodos, y obtener un único grafo desde ellos.

START:
    CREATE SET<STRING> s1
    INSERT INTO s1 STRING "1"

    CREATE SET<STRING> s2
    INSERT INTO s1 STRING "3"

    CREATE GRAPH<EMPTY, EMPTY> g
    INSERT INTO g SET<STRING> s1
    INSERT INTO g SET<STRING> s2
